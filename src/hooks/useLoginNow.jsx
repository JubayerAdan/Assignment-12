import React from "react";
import { Navigate } from "react-router-dom";

const useLoginNow = () => {
  const isLogined = true;
  if (isLogined) {
    return ;
  }
  if (!isLogined) {
    
  }
};

export default useLoginNow;
